import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tamaes.ionic.kontak',
  appName: 'kontak',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
